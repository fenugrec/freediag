// cconf.h.in
// test to replace autotools' config.h.in + config.h files. 
//usually @varname@ will be replace by the contents of the cmake variable varname

// # cmakedefine asdf @varname@ will "#define asdf 1" if @varname@ is non-false ?
#cmakedefine DONT_USE_RCFILE @NUSE_RCFILE@
#cmakedefine OLD_DEVNAME @OLDEV@

#define PACKAGE_VERSION @PKGVERSION@

//do we need to manually add all the #defines that autotools did ?