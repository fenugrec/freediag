#very simple sanity check of the CLI and testsuite.
set 
show	
 up
	diag
?
#usage in subcmd, must not leave level
probe ?
?
up
#usage through subcmd
diag probe ?

diag
#cmd error in subcmd, must not leave level
sr 2
up


#unavailable cmd @ root. stops processing this file, which means we can't quit anymore.
#probe
#unavailabe cmd through subcmd table, same problem
#diag badcmd

quit